import {
  StyleSheet,
  // Platform,
} from 'react-native'

const styles = StyleSheet.create({
  containerStyle: {
    backgroundColor: '#FFFFFF',
    borderWidth: 1,
    borderRadius: 5,
    borderColor: '#DDDDDD',
    borderBottomWidth: 0,
    shadowColor: '#000000',
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 5,
    marginHorizontal: 5,
    marginTop: 10,
    padding: 10,
  },
})

export default styles
