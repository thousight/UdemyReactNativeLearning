import {
  StyleSheet,
  // Platform,
} from 'react-native'

const styles = StyleSheet.create({
  containerStyle: {
    backgroundColor: '#FFFFFF',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    padding: 5,
    position: 'relative',
  },
})

export default styles
